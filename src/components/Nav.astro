---
interface Props {
  page: 'work' | 'about' | 'career' | 'fun';
}
const { page } = Astro.props;
const base = import.meta.env.BASE_URL;
const workHref = page === 'work' ? '#work' : base;
---
<nav class="navigation">
  <a href={workHref} class={`nav-link${page === 'work' ? ' active' : ''}`} aria-current={page === 'work' ? 'page' : undefined}>Work</a>
  <a href={`${base}career.html`} class={`nav-link${page === 'career' ? ' active' : ''}`} aria-current={page === 'career' ? 'page' : undefined}>Career</a>
  <a href={`${base}fun.html`} class={`nav-link${page === 'fun' ? ' active' : ''}`} aria-current={page === 'fun' ? 'page' : undefined}>Fun</a>
  <a href={`${base}about.html`} class={`nav-link${page === 'about' ? ' active' : ''}`} aria-current={page === 'about' ? 'page' : undefined}>About</a>
  <button type="button" class="nav-link" commandfor="cvPicker" command="toggle-popover">Resume</button>
  <button type="button" class="theme-toggle" id="themeToggle" aria-label="Switch to dark mode" aria-pressed="false">
    <svg class="icon-moon" width="18" height="18" aria-hidden="true"><use href="#icon-moon"/></svg>
    <svg class="icon-sun" width="18" height="18" aria-hidden="true"><use href="#icon-sun"/></svg>
  </button>
</nav>
